# Frontend

Next.js will live here after the PGN -> Analysis -> Chess DNA backend loop is working.

For MVP validation, this folder intentionally stays light. The first product screen should be a premium import-and-report workflow, not a landing page or chess platform shell.

