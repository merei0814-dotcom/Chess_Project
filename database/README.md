# Database

PostgreSQL is the production database for CHESS AI COACH.

The initial schema is in:

- `schema.sql`
- `migrations/001_initial.sql`

The FastAPI backend can also run locally on SQLite by default so the PGN analysis slice can be tested before PostgreSQL is configured.

